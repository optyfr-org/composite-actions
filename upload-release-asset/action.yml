name: Upload release asset
description: Uploads files, or files matching a glob, to a GitHub release

inputs:
  tag:
    description: Release tag name
    required: true
  file:
    description: Path or glob of the file(s) to upload
    required: true
  repo:
    description: GitHub repository (owner/repo)
    required: false
    default: ${{ github.repository }}
  token:
    description: GitHub token for authentication
    required: true


runs:
  using: composite
  steps:
    - name: Setup release name
      id: setup
      shell: bash
      env:
        TAG: ${{ inputs.tag }}
        REPO: ${{ inputs.repo }}
      run: |
          echo "release_owner=${REPO%%/*}" >> $GITHUB_OUTPUT
          echo "release_repo=${REPO#*/}" >> $GITHUB_OUTPUT
          if [ -n "${TAG}" ]; then
            channel="release"
            version="${TAG#v}"
            echo "create_release=false" >> $GITHUB_OUTPUT
            echo "release_name=$channel-$version" >> $GITHUB_OUTPUT
            echo "release_channel=$channel" >> $GITHUB_OUTPUT
            echo "release_version=$version" >> $GITHUB_OUTPUT
          else
            channel="nightly"
            version="$(date -u +%Y%m%d)"
            echo "create_release=true" >> $GITHUB_OUTPUT
            echo "release_name=$channel-$version" >> $GITHUB_OUTPUT
            echo "release_channel=$channel" >> $GITHUB_OUTPUT
            echo "release_version=$version" >> $GITHUB_OUTPUT
          fi

    - name: Update Nightly tag
      if: ${{ steps.setup.outputs.create_release == 'true' && steps.setup.outputs.release_channel == 'nightly' }}
      shell: bash
      env:
        TOKEN: ${{ inputs.token }}
        REPO: ${{ inputs.repo }}
      run: |
          git tag -f nightly
          git push "https://x-access-token:${TOKEN}@github.com/${REPO}.git" -f nightly

    - name: Update Nightly release
      if: ${{ steps.setup.outputs.create_release == 'true' && steps.setup.outputs.release_channel == 'nightly' }}
      uses: 'ncipollo/release-action@339a81892b84b4eeb0f6e744e4574d79d0d9b8dd'
      with:
        prerelease: true
        allowUpdates: true
        removeArtifacts: true
        replacesArtifacts: true
        generateReleaseNotes: true
        name: ${{ steps.setup.outputs.release_name }}
        tag: nightly
        artifacts: ${{ inputs.file }}
        owner: ${{ steps.setup.outputs.release_owner }}
        repo: ${{ steps.setup.outputs.release_repo }}
        token: ${{ inputs.token }}

    - name: "Release ${{ steps.setup.outputs.release_version }}"
      if: ${{ steps.setup.outputs.create_release == 'true' && steps.setup.outputs.release_channel == 'release' }}
      uses: ncipollo/release-action@339a81892b84b4eeb0f6e744e4574d79d0d9b8dd
      with:
        draft: true
        prerelease: false
        makeLatest: true
        name: Release - ${{ steps.setup.outputs.release_version }}
        tag: ${{ steps.setup.outputs.release_name }}
        artifacts: ${{ inputs.file }}
        owner: ${{ steps.setup.outputs.release_owner }}
        repo: ${{ steps.setup.outputs.release_repo }}
        token: ${{ inputs.token }}
